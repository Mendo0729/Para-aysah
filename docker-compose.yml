services:
  para-aysah:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: para-aysah
    restart: unless-stopped
    ports:
      - "3010:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
